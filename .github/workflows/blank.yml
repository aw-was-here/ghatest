name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: ps
      run: ps -ef
    - name: ls
      run: ls -alR
    - name: df
      run: df -h      
    
