name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: ls
      run: ls -alR
    - name: df
      run: df -h
    - name: pwd
      run: pwd      
    - name: env
      run: env
